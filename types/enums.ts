export enum PermitStatus {
  REQUESTED = "REQUESTED",
  APPROVED = "APPROVED",
  EXPIRED = "EXPIRED",
  SUSPEND = "SUSPEND",
  ISSUED = "ISSUED",
}

export enum FacilityType {
  CART = "CART",
  TRUCK = "TRUCK",
  OTHER = "OTHER",
}
